# Stage Design Builder

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

If npm was previously pointed at a private registry, reset it with:

```bash
npm config set registry https://registry.npmjs.org/
```
